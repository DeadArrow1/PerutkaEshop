@section Styles
{
    <link rel="stylesheet" href="~/css/shop_item.css" />
}
@model Product

@{
    ViewData["Title"] = "Product Detail";
}

<section class="py-5">
    <div class="container px-4 px-lg-5 my-5">
        <div class="row gx-4 gx-lg-5 align-items-center">
            <div class="col-md-6"><img class="card-img-top mb-5 mb-md-0" src="@Model.ImageSource" alt="@Model.ImageAlt." /></div>
            <div class="col-md-6">
                <div class="small mb-1">SKU: BST-498</div>
                <h1 class="display-5 fw-bolder">@Model.Name</h1>
                <div class="fs-5 mb-5">
                    <span>@Model.Price</span>
                </div>
                <p class="lead">@Model.Description</p>
                <div class="d-flex">
                    <button class="btn btn-outline-dark flex-shrink-0" type="button"
                            onclick="Buy(@Model.ID,'@Url.Action("AddOrderItemsToSession", "CustomerOrderNotCart", new { Area = "Customer" })', '#total_price', '@System.Globalization.CultureInfo.CurrentCulture.Name')">
                        <i class="bi-cart-fill me-1"></i>
                        Buy
                    </button>
                </div>
            </div>
        </div>
    </div>
</section>
    @section Scripts
{
        <enviroment include="Development">
            <script src="~/js/Product.js"></script>
        </enviroment>
        <enviroment exclude="Development">
            <script src="~/js/Product.min.js"></script>
        </enviroment>

    }
